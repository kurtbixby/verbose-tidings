{{!-- Title + body + User + Date --}}
<div class="bg-cyan-300 border-cyan-300 border-4 rounded-md">
    <div>
        <div class="flex justify-between mx-2">
            <h2>{{title}}</h2>
            <div>{{user.username}} {{date}} at {{time}}</div>
        </div>
    </div>
    <p class="bg-slate-500 p-2 rounded-md m-1">{{body}}</p>
</div>